# Copy to .env and fill in. Never commit .env.
RUNWAY_API_KEY=
ELEVENLABS_API_KEY=
# Optional: skip --voice-id on synth-narration once set. Run
# `ai-video-pipeline list-voices` to pick one.
ELEVENLABS_VOICE_ID=
